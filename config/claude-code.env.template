# Generated by ollama-claude. Source this file to point Claude Code at a
# local Ollama endpoint. Restart Claude Code after sourcing.
#
# This file is regenerated by `oc install` and `oc topology`. Do not edit
# by hand; edit the source TOML and re-run instead.

export ANTHROPIC_BASE_URL="__BASE_URL__"
export ANTHROPIC_AUTH_TOKEN="ollama"

# Some Claude Code versions treat ANTHROPIC_API_KEY="" as "missing key" and
# fail fast. `oc doctor` confirms which form works on this version; the
# installer picks the right one. The line below is the default.
unset ANTHROPIC_API_KEY
